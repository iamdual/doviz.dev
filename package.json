{
  "name": "doviz.dev",
  "version": "0.0.1",
  "scripts": {
    "generate:api": "node ./src/generate-api.js",
    "generate:docs": "node ./src/generate-docs.js",
    "generate": "npm run generate:api && npm run generate:docs"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "cheerio": "^1.0.0",
    "fast-xml-parser": "^4.5.0",
    "showdown": "^2.1.0"
  }
}
